// 💀 [留白]：用于定义角色与 TTS 模型的映射关系
// 示例结构：
export const VOICE_CONFIG: Record<string, any> = {
  /* 
  "sakiko": {
    gpt_weights: "path/to/gpt.ckpt",
    sovits_weights: "path/to/sovits.pth",
    ref_audio: "ref.wav",
    ref_text: "Reference text here."
  } 
  */
};

export const VOICE_DEFAULTS = {};